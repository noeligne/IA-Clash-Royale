# HOW TO RUN:

1. Clone the repository:
```bash
git clone git@github.com:noeligne/IA-Clash-Royale.git
```

2. Set up Python Environment:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

3. Run the program
